import { Route } from '@angular/router';
import { AngularFormComponent } from './angular-form/angular-form.component';

export const angularFormRoutes: Route[] = [
  { path: '', component: AngularFormComponent },
];
